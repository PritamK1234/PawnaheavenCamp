import { PWAInstallButton as UnifiedPWAInstallButton } from '@/components/PWAInstallButton';

const PWAInstallButton = () => {
  return <UnifiedPWAInstallButton variant="compact" />;
};

export default PWAInstallButton;
